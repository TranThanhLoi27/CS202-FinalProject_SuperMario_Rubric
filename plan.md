request: My goal.png has been changed to 170 * 256, since the character already has a frame when attacking, so please remove the colored image representing the attack from before. Also, I want the selector frame to be larger (4 times horizontally and 3 times vertically). And the inventory logic is missing the count when picking up the corresponding item. After each match, 30 coins will be automatically added to your inventory, and defeating monsters will have a higher chance of dropping more coins. thêm flyingenemy cho tôi quái này sẽ bay trên không tương ứng với chữ F trong các file level.txt được đọc bởi levelloader
 
 
 previous request: I have updated selector.png to contain 2 consecutive frames (512 × 192 each)—the first for regular frames and the second for the selected frame—and I need you to center these frames on the 1280 × 720 screen with all text perfectly centered inside each frame. Please fix the background rendering bug where map blocks incorrectly appear in the Main Menu, Character Selection, and Map Selection screens. Implement monster drop mechanics using food.png upon death, and add rendering logic for monsters matching their respective names and dimensions (patrol.png, shooter.png, flying.png at 32 × 48, and boss.png at 72 × 78), with shooter and boss projectiles sourced from boss_projectile.png (4 frames, 15 × 15 each). Add an inventory system below the health and hunger bars with 6 slots: slots 1–4 holding specific items/food (15 × 15 from food.png), slot 5 holding coins (15 × 15 across 6 frames from coin.png), and slot 6 holding solid blocks (solid.png) with a starting count of 2 per player that can be visualized and placed onto the map. Finally, implement slot-switching controls with action timers use for đồ vật đang được chọn sẽ tương ứng (eating, dropping coins, and placing blocks), tie food consumption to hunger restoration (5%, 10%, 15%, 20%) and minor health recovery, and ensure player money persists upon map completion.
And my character gets an additional frame (for a total of 6 frames) of the same size as the previous frames, and this frame appears when attacking, set logic for this.


